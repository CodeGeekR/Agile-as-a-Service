---
import Layout from '../layouts/Layout.astro';
import LanguageSelector from '../components/LanguageSelector.jsx';
---

<Layout title="AaaS - Agile as a Service | Optimiza tu Existencia">
  <!-- Bolt Logo Flotante - Posición corregida -->
  <div class="fixed top-24 right-4 z-40 bolt-logo-container">
    <a 
      href="https://bolt.new/" 
      target="_blank" 
      rel="noopener noreferrer"
      class="block w-16 h-16 hover:scale-110 transition-transform duration-300 hover:shadow-lg rounded-full overflow-hidden"
      title="Powered by Bolt.new"
    >
      <img 
        src="/black_circle_360x360.webp" 
        alt="Powered by Bolt" 
        class="w-full h-full object-cover"
        loading="eager"
      />
    </a>
  </div>

  <!-- Header -->
  <header class="relative bg-gradient-to-br from-primary via-blue-700 to-purple-800 text-white overflow-hidden">
    <!-- Background Pattern -->
    <div class="absolute inset-0 bg-grid-pattern bg-grid opacity-10"></div>
    <div class="absolute inset-0 bg-gradient-to-br from-blue-600/20 via-purple-600/20 to-indigo-800/20"></div>
    
    <!-- Floating Elements -->
    <div class="absolute top-10 left-10 w-20 h-20 bg-white/10 rounded-full animate-float"></div>
    <div class="absolute top-32 right-20 w-16 h-16 bg-white/5 rounded-full animate-float-delayed"></div>
    <div class="absolute bottom-20 left-1/4 w-12 h-12 bg-white/10 rounded-full animate-blob"></div>
    
    <!-- Navigation -->
    <nav class="relative z-50 px-4 sm:px-6 lg:px-8 py-4">
      <div class="flex items-center justify-between">
        <!-- Logo -->
        <div class="flex items-center space-x-3">
          <div class="w-10 h-10 bg-white rounded-xl flex items-center justify-center shadow-lg">
            <span class="text-primary font-bold text-lg">A</span>
          </div>
          <div class="hidden sm:block">
            <h1 class="text-xl font-bold tracking-tight">AaaS</h1>
            <p class="text-xs text-blue-100 -mt-1">Agile as a Service</p>
          </div>
        </div>
        
        <!-- Navigation Links -->
        <div class="hidden md:flex items-center space-x-8">
          <a href="#features" class="text-blue-100 hover:text-white transition-colors text-sm font-medium" data-i18n="hero.features">Características</a>
          <a href="#demo" class="text-blue-100 hover:text-white transition-colors text-sm font-medium" data-i18n="hero.demo">Demo</a>
          <a href="#testimonials" class="text-blue-100 hover:text-white transition-colors text-sm font-medium" data-i18n="hero.testimonials">Testimonios</a>
          <a href="/dashboard" class="bg-white text-primary px-4 py-2 rounded-lg font-medium hover:bg-blue-50 transition-colors text-sm">Dashboard</a>
        </div>
        
        <!-- Language Selector - Z-index alto para estar sobre el logo -->
        <div id="language-selector-container" class="relative z-50">
          <!-- Language selector will be rendered here -->
        </div>
      </div>
    </nav>

    <!-- Hero Section -->
    <div class="relative z-10 px-4 sm:px-6 lg:px-8 py-16 lg:py-24">
      <div class="max-w-7xl mx-auto">
        <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
          <!-- Left Column - Content -->
          <div class="text-center lg:text-left">
            <!-- Badge -->
            <div class="hero-badge inline-flex items-center bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-4 py-2 mb-8">
              <span class="w-2 h-2 bg-green-400 rounded-full mr-3 animate-pulse"></span>
              <span class="text-sm font-medium" data-i18n="hero.demoAvailable">DEMO PÚBLICO DISPONIBLE</span>
            </div>
            
            <!-- Main Title -->
            <h1 class="hero-title text-4xl sm:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
              <span class="block" data-i18n="hero.title">Agile as a Service</span>
            </h1>
            
            <!-- Subtitle -->
            <p class="hero-subtitle text-lg sm:text-xl text-blue-100 mb-8 leading-relaxed max-w-2xl mx-auto lg:mx-0" data-i18n="hero.subtitle">
              Optimiza tu existencia con el framework que está revolucionando la ineficiencia. Transforma el caos de tu vida en un backlog perfectamente organizado de decepciones predecibles.
            </p>
            
            <!-- CTA Buttons -->
            <div class="hero-buttons flex flex-col sm:flex-row gap-4 justify-center lg:justify-start mb-12">
              <button onclick="showAuthModal('register')" class="bg-white text-primary px-8 py-4 rounded-xl font-semibold hover:bg-blue-50 transition-all duration-300 shadow-lg hover:shadow-xl hover:scale-105 text-lg">
                <span data-i18n="hero.startSprint">Comenzar Sprint Gratuito</span>
              </button>
              <button onclick="showAuthModal('demo')" class="border-2 border-white text-white px-8 py-4 rounded-xl font-semibold hover:bg-white hover:text-primary transition-all duration-300 text-lg">
                <span data-i18n="hero.viewDemo">Ver Demo Completo</span>
              </button>
            </div>
            
            <!-- Demo Access Card -->
            <div class="hero-demo-card bg-white/10 backdrop-blur-sm border border-white/20 rounded-2xl p-6 max-w-md mx-auto lg:mx-0">
              <h3 class="font-semibold mb-3 text-lg" data-i18n="hero.tryPlatform">Prueba la plataforma completa sin registro:</h3>
              <div class="space-y-2 text-sm">
                <div class="flex items-center justify-between">
                  <span data-i18n="hero.email">Email:</span>
                  <code class="bg-white/20 px-2 py-1 rounded text-blue-100">demo@aaas.com</code>
                </div>
                <div class="flex items-center justify-between">
                  <span data-i18n="hero.password">Password:</span>
                  <code class="bg-white/20 px-2 py-1 rounded text-blue-100">demo123</code>
                </div>
              </div>
              <p class="text-xs text-blue-200 mt-3" data-i18n="hero.orCreate">O usa cualquier email/contraseña para crear tu cuenta</p>
            </div>
          </div>
          
          <!-- Right Column - Visual -->
          <div class="relative">
            <!-- Main Dashboard Preview -->
            <div class="bg-white rounded-2xl shadow-2xl p-6 transform rotate-3 hover:rotate-0 transition-transform duration-500">
              <div class="flex items-center space-x-2 mb-4">
                <div class="w-3 h-3 bg-red-400 rounded-full"></div>
                <div class="w-3 h-3 bg-yellow-400 rounded-full"></div>
                <div class="w-3 h-3 bg-green-400 rounded-full"></div>
              </div>
              <div class="space-y-3">
                <div class="h-4 bg-gray-200 rounded w-3/4"></div>
                <div class="h-3 bg-gray-100 rounded w-1/2"></div>
                <div class="grid grid-cols-3 gap-2 mt-4">
                  <div class="h-16 bg-blue-100 rounded"></div>
                  <div class="h-16 bg-yellow-100 rounded"></div>
                  <div class="h-16 bg-green-100 rounded"></div>
                </div>
                <div class="h-2 bg-gray-200 rounded w-full mt-4"></div>
                <div class="h-2 bg-blue-500 rounded w-2/3"></div>
              </div>
            </div>
            
            <!-- Floating Cards -->
            <div class="absolute -top-4 -left-4 bg-red-500 text-white p-3 rounded-lg shadow-lg transform -rotate-12 animate-float">
              <span class="text-xs font-bold">🔥 CRISIS</span>
            </div>
            <div class="absolute -bottom-4 -right-4 bg-green-500 text-white p-3 rounded-lg shadow-lg transform rotate-12 animate-float-delayed">
              <span class="text-xs font-bold">✅ DONE</span>
            </div>
          </div>
        </div>
        
        <!-- Trusted By Section -->
        <div class="hero-brands mt-16 lg:mt-24">
          <p class="text-center text-blue-200 text-sm mb-8" data-i18n="hero.trustedBy">Confían en nosotros:</p>
          <div class="brands-container relative overflow-hidden">
            <div class="brands-track flex items-center space-x-12">
              <!-- Marcas que se repiten para el efecto infinito -->
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Atlassian</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Microsoft</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Google</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Meta</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Amazon</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Netflix</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Spotify</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Tesla</span><span class="text-xs">*</span>
              </div>
              <!-- Duplicamos las marcas para el efecto infinito -->
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Atlassian</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Microsoft</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Google</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Meta</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Amazon</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Netflix</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Spotify</span><span class="text-xs">*</span>
              </div>
              <div class="brand-item flex items-center space-x-2 text-blue-200 whitespace-nowrap">
                <span class="font-semibold">Tesla</span><span class="text-xs">*</span>
              </div>
            </div>
          </div>
          <p class="text-center text-blue-300 text-xs mt-4" data-i18n="hero.notReally">*No realmente, pero podrían</p>
        </div>
      </div>
    </div>
  </header>

  <!-- Features Section -->
  <section id="features" class="py-20 lg:py-32 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Section Header -->
      <div class="features-header text-center mb-16 lg:mb-24">
        <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-900 mb-6" data-i18n="features.title">
          ¿Por qué complicar lo simple cuando puedes hacer lo imposible?
        </h2>
        <p class="text-xl text-gray-600 max-w-4xl mx-auto leading-relaxed" data-i18n="features.subtitle">
          Nuestro framework patentado convierte tareas de 5 minutos en épicas de múltiples sprints, garantizando la máxima burocracia en cada aspecto de tu vida diaria.
        </p>
      </div>

      <!-- Features Grid -->
      <div class="grid md:grid-cols-3 gap-8 lg:gap-12">
        <!-- Feature 1 -->
        <div class="feature-card group">
          <div class="bg-white rounded-2xl p-8 shadow-soft hover:shadow-large transition-all duration-300 group-hover:-translate-y-2 border border-gray-100">
            <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-4" data-i18n="features.epics.title">Épicas de Vida™</h3>
            <p class="text-gray-600 leading-relaxed" data-i18n="features.epics.description">
              Convierte "hacer café" en una épica de 34 historias de usuario. Incluye análisis de stakeholders, research de mercado de cafeterías y retrospectiva post-cafeína.
            </p>
          </div>
        </div>

        <!-- Feature 2 -->
        <div class="feature-card group">
          <div class="bg-white rounded-2xl p-8 shadow-soft hover:shadow-large transition-all duration-300 group-hover:-translate-y-2 border border-gray-100">
            <div class="w-16 h-16 bg-gradient-to-br from-red-500 to-pink-600 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-4" data-i18n="features.burndown.title">Burndown Existencial</h3>
            <p class="text-gray-600 leading-relaxed" data-i18n="features.burndown.description">
              Gráficos detallados que muestran cómo tu felicidad decrece proporcionalmente al avance del sprint. Incluye métricas de ansiedad y desesperanza.
            </p>
          </div>
        </div>

        <!-- Feature 3 -->
        <div class="feature-card group">
          <div class="bg-white rounded-2xl p-8 shadow-soft hover:shadow-large transition-all duration-300 group-hover:-translate-y-2 border border-gray-100">
            <div class="w-16 h-16 bg-gradient-to-br from-green-500 to-teal-600 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-4" data-i18n="features.ceremonies.title">Ceremonias Forzadas</h3>
            <p class="text-gray-600 leading-relaxed" data-i18n="features.ceremonies.description">
              Daily stand-ups obligatorios contigo mismo. Retrospectivas semanales sobre por qué no lograste tus objetivos. Planning poker para decidir cuántos puntos vale ducharse.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Demo Access Section -->
  <section id="demo" class="py-20 lg:py-32 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="demo-access-card bg-gradient-to-br from-blue-50 to-indigo-100 rounded-3xl p-8 lg:p-12 border border-blue-200">
        <div class="w-20 h-20 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl flex items-center justify-center mx-auto mb-8">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
        </div>
        
        <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-6" data-i18n="demoAccess.title">
          Acceso Demo Instantáneo
        </h2>
        
        <p class="text-xl text-gray-600 mb-8 leading-relaxed" data-i18n="demoAccess.description">
          Prueba todas las funcionalidades de AaaS sin necesidad de registro. Usa estas credenciales públicas para acceder inmediatamente:
        </p>
        
        <div class="bg-white rounded-2xl p-6 mb-8 shadow-medium border border-gray-200">
          <div class="grid sm:grid-cols-2 gap-4 mb-6">
            <div class="text-left">
              <label class="block text-sm font-semibold text-gray-700 mb-2" data-i18n="demoAccess.emailDemo">Email Demo:</label>
              <code class="block bg-gray-100 px-4 py-3 rounded-lg text-gray-800 font-mono">demo@aaas.com</code>
            </div>
            <div class="text-left">
              <label class="block text-sm font-semibold text-gray-700 mb-2" data-i18n="demoAccess.passwordDemo">Password Demo:</label>
              <code class="block bg-gray-100 px-4 py-3 rounded-lg text-gray-800 font-mono">demo123</code>
            </div>
          </div>
          
          <button onclick="showAuthModal('demo')" class="w-full bg-gradient-to-r from-blue-600 to-purple-600 text-white px-8 py-4 rounded-xl font-semibold hover:from-blue-700 hover:to-purple-700 transition-all duration-300 shadow-lg hover:shadow-xl text-lg">
            <span data-i18n="demoAccess.accessNow">🚀 Acceder al Demo Ahora</span>
          </button>
        </div>
        
        <p class="text-sm text-gray-500 leading-relaxed" data-i18n="demoAccess.includes">
          Dashboard completo • Kanban interactivo • Métricas en tiempo real • Stand-ups forzados • Y mucho más sufrimiento metodológico
        </p>
      </div>
    </div>
  </section>

  <!-- Testimonials Section -->
  <section id="testimonials" class="py-20 lg:py-32 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Section Header -->
      <div class="testimonials-header text-center mb-16 lg:mb-24">
        <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-900 mb-6" data-i18n="testimonials.title">
          Lo que dicen nuestros usuarios desesperados
        </h2>
      </div>

      <!-- Testimonials Grid -->
      <div class="grid md:grid-cols-3 gap-8 lg:gap-12">
        <!-- Testimonial 1 -->
        <div class="testimonial-card">
          <div class="bg-white rounded-2xl p-8 shadow-soft hover:shadow-large transition-all duration-300 border border-gray-100">
            <div class="flex items-center mb-6">
              <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full flex items-center justify-center mr-4">
                <span class="text-white font-bold">MR</span>
              </div>
              <div>
                <h4 class="font-semibold text-gray-900">María Rodríguez</h4>
                <p class="text-sm text-gray-500" data-i18n="testimonials.role1">Senior Life Engineer</p>
              </div>
            </div>
            <blockquote class="text-gray-600 leading-relaxed" data-i18n="testimonials.testimonial1">
              "Antes tardaba 30 segundos en cepillarme los dientes. Ahora tengo un backlog de 47 subtareas y 3 dependencias externas. ¡Nunca había sido tan productivamente miserable!"
            </blockquote>
          </div>
        </div>

        <!-- Testimonial 2 -->
        <div class="testimonial-card">
          <div class="bg-white rounded-2xl p-8 shadow-soft hover:shadow-large transition-all duration-300 border border-gray-100">
            <div class="flex items-center mb-6">
              <div class="w-12 h-12 bg-gradient-to-br from-green-500 to-teal-600 rounded-full flex items-center justify-center mr-4">
                <span class="text-white font-bold">CL</span>
              </div>
              <div>
                <h4 class="font-semibold text-gray-900">Carlos López</h4>
                <p class="text-sm text-gray-500" data-i18n="testimonials.role2">Product Owner de su propia vida</p>
              </div>
            </div>
            <blockquote class="text-gray-600 leading-relaxed" data-i18n="testimonials.testimonial2">
              "Mi sprint para 'hacer la cama' lleva 3 iteraciones. He identificado 12 impedimentos y estoy considerando contratar un Scrum Master para mi dormitorio."
            </blockquote>
          </div>
        </div>

        <!-- Testimonial 3 -->
        <div class="testimonial-card">
          <div class="bg-white rounded-2xl p-8 shadow-soft hover:shadow-large transition-all duration-300 border border-gray-100">
            <div class="flex items-center mb-6">
              <div class="w-12 h-12 bg-gradient-to-br from-red-500 to-pink-600 rounded-full flex items-center justify-center mr-4">
                <span class="text-white font-bold">AS</span>
              </div>
              <div>
                <h4 class="font-semibold text-gray-900">Ana Silva</h4>
                <p class="text-sm text-gray-500" data-i18n="testimonials.role3">Agile Coach Existencial</p>
              </div>
            </div>
            <blockquote class="text-gray-600 leading-relaxed" data-i18n="testimonials.testimonial3">
              "AaaS me ha enseñado que incluso respirar puede optimizarse con un framework Agile. Ahora tengo KPIs para cada inhalación."
            </blockquote>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 lg:py-32 bg-gradient-to-br from-primary via-blue-700 to-purple-800 text-white relative overflow-hidden">
    <!-- Background Elements -->
    <div class="absolute inset-0 bg-grid-pattern bg-grid opacity-10"></div>
    <div class="absolute top-20 left-20 w-32 h-32 bg-white/5 rounded-full animate-blob"></div>
    <div class="absolute bottom-20 right-20 w-24 h-24 bg-white/10 rounded-full animate-float"></div>
    
    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="cta-content">
        <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold mb-6 leading-tight" data-i18n="cta.title">
          ¿Listo para transformar tu vida en un proyecto fallido perfectamente documentado?
        </h2>
        
        <p class="text-xl text-blue-100 mb-12 leading-relaxed max-w-3xl mx-auto" data-i18n="cta.subtitle">
          Únete a los miles de profesionales que ya han convertido su existencia en un desastre metodológicamente impecable.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-6 justify-center mb-8">
          <button onclick="showAuthModal('register')" class="bg-white text-primary px-8 py-4 rounded-xl font-semibold hover:bg-blue-50 transition-all duration-300 shadow-lg hover:shadow-xl hover:scale-105 text-lg">
            <span data-i18n="cta.startFree">Comenzar Sprint Gratuito</span>
          </button>
          <button onclick="showAuthModal('contact')" class="border-2 border-white text-white px-8 py-4 rounded-xl font-semibold hover:bg-white hover:text-primary transition-all duration-300 text-lg">
            <span data-i18n="cta.talkConsultant">Hablar con un Consultor</span>
          </button>
        </div>
        
        <p class="text-sm text-blue-200" data-i18n="cta.noCommitment">
          Sin compromiso. Cancela cuando te des cuenta de lo absurdo de todo esto.
        </p>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
      <div class="footer-content grid md:grid-cols-4 gap-8 lg:gap-12">
        <!-- Brand Column -->
        <div class="footer-brand md:col-span-1">
          <div class="flex items-center space-x-3 mb-6">
            <div class="w-10 h-10 bg-primary rounded-xl flex items-center justify-center">
              <span class="text-white font-bold text-lg">A</span>
            </div>
            <div>
              <h3 class="text-xl font-bold">AaaS</h3>
              <p class="text-gray-400 text-sm">Agile as a Service</p>
            </div>
          </div>
          <p class="text-gray-400 text-sm leading-relaxed mb-6" data-i18n="footer.tagline">
            Revolucionando la ineficiencia desde 2025.
          </p>
          
          <!-- Developer Info -->
          <div class="border-t border-gray-800 pt-6">
            <h4 class="text-sm font-semibold text-gray-300 mb-3">Desarrollado por:</h4>
            <a 
              href="https://pythonweb.is-a.dev/" 
              target="_blank" 
              rel="noopener noreferrer"
              class="inline-flex items-center space-x-2 text-blue-400 hover:text-blue-300 transition-colors"
            >
              <span class="font-medium">samuraidev</span>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
              </svg>
            </a>
          </div>
        </div>

        <!-- Product Links -->
        <div class="footer-links">
          <h4 class="font-semibold mb-6" data-i18n="footer.product">Producto</h4>
          <ul class="space-y-3 text-sm">
            <li><a href="#features" class="text-gray-400 hover:text-white transition-colors" data-i18n="footer.features">Características</a></li>
            <li><a href="#" class="text-gray-400 hover:text-white transition-colors" data-i18n="footer.pricing">Precios</a></li>
            <li><a href="#" class="text-gray-400 hover:text-white transition-colors" data-i18n="footer.integrations">Integraciones</a></li>
          </ul>
        </div>

        <!-- Company Links -->
        <div class="footer-links">
          <h4 class="font-semibold mb-6" data-i18n="footer.company">Empresa</h4>
          <ul class="space-y-3 text-sm">
            <li><a href="#" class="text-gray-400 hover:text-white transition-colors" data-i18n="footer.about">Sobre nosotros</a></li>
            <li><a href="#" class="text-gray-400 hover:text-white transition-colors" data-i18n="footer.careers">Carreras</a></li>
            <li><a href="#" class="text-gray-400 hover:text-white transition-colors" data-i18n="footer.press">Prensa</a></li>
          </ul>
        </div>

        <!-- Support Links -->
        <div class="footer-links">
          <h4 class="font-semibold mb-6" data-i18n="footer.support">Soporte</h4>
          <ul class="space-y-3 text-sm">
            <li><a href="#" class="text-gray-400 hover:text-white transition-colors" data-i18n="footer.documentation">Documentación</a></li>
            <li><a href="#" class="text-gray-400 hover:text-white transition-colors" data-i18n="footer.guides">Guías</a></li>
            <li><a href="#" class="text-gray-400 hover:text-white transition-colors" data-i18n="footer.status">Estado del sistema</a></li>
          </ul>
        </div>
      </div>

      <!-- Bottom Section -->
      <div class="border-t border-gray-800 mt-12 pt-8">
        <div class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
          <div class="text-sm text-gray-400">
            <p data-i18n="footer.copyright">
              © 2025 AaaS. Todos los derechos innecesariamente reservados.
            </p>
            <p class="mt-1">
              Participando en el <strong>Useless Sh!t Challenge</strong> organizado por <a href="https://bolt.new/" target="_blank" rel="noopener noreferrer" class="text-blue-400 hover:text-blue-300 transition-colors font-medium">Bolt</a>
            </p>
          </div>
          
          <!-- Social Links -->
          <div class="flex items-center space-x-4">
            <a href="https://bolt.new/" target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:text-white transition-colors">
              <span class="sr-only">Bolt</span>
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </a>
            <a href="https://pythonweb.is-a.dev/" target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:text-white transition-colors">
              <span class="sr-only">Developer</span>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/>
              </svg>
            </a>
          </div>
        </div>
      </div>
    </div>
  </footer>

  <!-- Auth Modal Container -->
  <div id="auth-modal-container"></div>
</Layout>

<style>
  /* Reglas universales para garantizar visibilidad */
  * {
    opacity: 1 !important;
    visibility: visible !important;
  }

  /* Elementos específicos asegurados */
  .hero-badge, .hero-title, .hero-subtitle, .hero-buttons,
  .hero-demo-card, .hero-brands, .features-header, .feature-card,
  .demo-access-card, .testimonials-header, .testimonial-card,
  .cta-content, .footer-content, .footer-brand, .footer-links,
  #language-selector-container, #language-toggle,
  button, a, h1, h2, h3, h4, h5, h6, p, span, div {
    opacity: 1 !important;
    visibility: visible !important;
  }

  /* Bolt Logo Styles - Posición ajustada para no bloquear selector */
  .bolt-logo-container {
    animation: float 6s ease-in-out infinite;
  }

  .bolt-logo-container:hover {
    animation-play-state: paused;
  }

  /* Brands Animation */
  .brands-container {
    height: 60px;
    mask-image: linear-gradient(to right, transparent, black 20%, black 80%, transparent);
    -webkit-mask-image: linear-gradient(to right, transparent, black 20%, black 80%, transparent);
  }

  .brands-track {
    animation: scroll-brands 20s linear infinite;
    width: max-content;
  }

  @keyframes scroll-brands {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  .brand-item {
    min-width: 150px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .bolt-logo-container {
      top: 20px;
      right: 16px;
    }
    
    .bolt-logo-container a {
      width: 3rem;
      height: 3rem;
    }
  }

  /* Language selector dropdown z-index fix */
  #language-dropdown {
    z-index: 60 !important;
  }

  /* Ensure all elements are visible on load */
  body {
    opacity: 1 !important;
    visibility: visible !important;
  }

  header, main, footer, nav, section, div, p, h1, h2, h3, h4, h5, h6, span, a, button {
    opacity: 1 !important;
    visibility: visible !important;
  }
</style>

<script>
  // Import i18n and components
  import('../i18n/index.js').then(i18nModule => {
    const { t, getLocale, setLocale } = i18nModule;
    
    // Render language selector
    const container = document.getElementById('language-selector-container');
    if (container) {
      import('../components/LanguageSelector.jsx').then(module => {
        const LanguageSelector = module.default;
        // Render the component (simplified for this context)
        renderLanguageSelector();
      });
    }
    
    function renderLanguageSelector() {
      const currentLocale = getLocale();
      const languages = [
        { code: 'es', name: 'Español', flag: '🇪🇸' },
        { code: 'en', name: 'English', flag: '🇺🇸' }
      ];
      const currentLanguage = languages.find(lang => lang.code === currentLocale);
      
      container.innerHTML = `
        <div class="relative">
          <button
            id="language-toggle"
            class="flex items-center space-x-2 px-3 py-2 text-sm text-blue-100 hover:text-white transition-colors rounded-lg hover:bg-white hover:bg-opacity-10"
          >
            <span class="text-lg">${currentLanguage?.flag}</span>
            <span class="hidden md:inline font-medium">${currentLanguage?.name}</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div id="language-dropdown" class="hidden absolute right-0 mt-2 w-48 bg-white rounded-xl shadow-lg border border-gray-200 z-60 overflow-hidden">
            <div class="py-2">
              ${languages.map(language => `
                <button
                  onclick="changeLanguage('${language.code}')"
                  class="w-full flex items-center space-x-3 px-4 py-3 text-sm text-left hover:bg-gray-50 transition-colors ${
                    currentLocale === language.code ? 'bg-blue-50 text-blue-600 font-medium' : 'text-gray-700'
                  }"
                >
                  <span class="text-lg">${language.flag}</span>
                  <span>${language.name}</span>
                  ${currentLocale === language.code ? `
                    <svg class="w-4 h-4 ml-auto text-blue-600" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                    </svg>
                  ` : ''}
                </button>
              `).join('')}
            </div>
            <div class="border-t border-gray-200 px-4 py-3">
              <p class="text-xs text-gray-500">
                ${currentLocale === 'es' ? 'Idioma detectado automáticamente' : 'Language auto-detected'}
              </p>
            </div>
          </div>
        </div>
      `;
      
      // Add event listeners
      document.getElementById('language-toggle').addEventListener('click', () => {
        const dropdown = document.getElementById('language-dropdown');
        dropdown.classList.toggle('hidden');
      });
      
      // Close dropdown when clicking outside
      document.addEventListener('click', (e) => {
        const dropdown = document.getElementById('language-dropdown');
        const toggle = document.getElementById('language-toggle');
        if (toggle && dropdown && !toggle.contains(e.target) && !dropdown.contains(e.target)) {
          dropdown.classList.add('hidden');
        }
      });
    }
    
    // Global function to change language
    window.changeLanguage = function(locale) {
      setLocale(locale);
      
      // Close dropdown
      const dropdown = document.getElementById('language-dropdown');
      if (dropdown) {
        dropdown.classList.add('hidden');
      }
      
      // Re-render language selector
      setTimeout(() => {
        renderLanguageSelector();
      }, 100);
    };
    
    // Update page content when language changes
    window.addEventListener('localeChanged', () => {
      updatePageContent();
    });
    
    function updatePageContent() {
      // Update elements with data-i18n
      document.querySelectorAll('[data-i18n]').forEach(element => {
        const key = element.getAttribute('data-i18n');
        element.textContent = t(key);
      });
    }
    
    // Initial content update
    updatePageContent();
  });

  // Auth Modal Functions
  window.showAuthModal = function(mode = 'login') {
    import('../components/AuthModal.jsx').then(module => {
      const AuthModal = module.default;
      
      // Create modal container
      const container = document.getElementById('auth-modal-container');
      container.innerHTML = `<div id="auth-modal-root"></div>`;
      
      // Render modal (simplified implementation)
      renderAuthModal(mode);
    });
  };
  
  function renderAuthModal(mode) {
    const container = document.getElementById('auth-modal-root');
    const isDemo = mode === 'demo';
    
    container.innerHTML = `
      <div class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 animate-fade-in">
        <div class="bg-white rounded-lg shadow-xl max-w-md w-full mx-4 animate-slide-up">
          <div class="bg-primary text-white p-6 rounded-t-lg">
            <div class="flex items-center justify-between">
              <h2 class="text-xl font-semibold">
                ${isDemo ? 'Acceso Demo' : mode === 'register' ? 'Crear Cuenta' : 'Iniciar Sesión'}
              </h2>
              <button onclick="closeAuthModal()" class="text-white hover:text-gray-200 transition-colors">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>
          </div>

          <form onsubmit="handleAuthSubmit(event, '${mode}')" class="p-6">
            ${mode === 'register' ? `
              <div class="mb-4">
                <label class="block text-sm font-medium text-text-primary mb-2">Nombre Completo</label>
                <input type="text" name="name" class="w-full p-3 border border-border-color rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent" placeholder="Tu nombre" required />
              </div>
            ` : ''}

            <div class="mb-4">
              <label class="block text-sm font-medium text-text-primary mb-2">Email</label>
              <input type="email" name="email" class="w-full p-3 border border-border-color rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent" 
                     placeholder="tu.email@empresa.com" value="${isDemo ? 'demo@aaas.com' : ''}" required />
            </div>

            <div class="mb-6">
              <label class="block text-sm font-medium text-text-primary mb-2">Contraseña</label>
              <input type="password" name="password" class="w-full p-3 border border-border-color rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent" 
                     placeholder="Mínimo 6 caracteres" value="${isDemo ? 'demo123' : ''}" required />
            </div>

            <button type="submit" class="w-full bg-primary hover:bg-primary-dark text-white py-3 px-4 rounded-lg font-medium transition-colors">
              ${isDemo ? 'Acceder al Demo' : mode === 'register' ? 'Crear Cuenta' : 'Iniciar Sesión'}
            </button>
          </form>
        </div>
      </div>
    `;
  }
  
  window.closeAuthModal = function() {
    document.getElementById('auth-modal-container').innerHTML = '';
  };
  
  window.handleAuthSubmit = function(e, mode) {
    e.preventDefault();
    const formData = new FormData(e.target);
    const email = formData.get('email');
    const password = formData.get('password');
    const name = formData.get('name') || 'Usuario Demo';
    
    // Simulate authentication
    const userData = {
      id: 1,
      email: email,
      name: name,
      role: 'Senior Life Engineer'
    };
    
    localStorage.setItem('aaas-auth-token', 'demo-token-' + Date.now());
    localStorage.setItem('aaas-user', JSON.stringify(userData));
    
    closeAuthModal();
    
    // Redirect based on mode
    if (mode === 'register') {
      window.location.href = '/onboarding';
    } else {
      window.location.href = '/dashboard';
    }
  };

  // Initialize animations with GSAP when available
  function initializeAnimations() {
    if (typeof gsap !== 'undefined') {
      try {
        // Hero elements animation
        const heroElements = document.querySelectorAll('.hero-badge, .hero-title, .hero-subtitle, .hero-buttons, .hero-demo-card');
        if (heroElements.length > 0) {
          gsap.from(heroElements, {
            duration: 1,
            y: 30,
            opacity: 0,
            stagger: 0.2,
            ease: "power2.out"
          });
        }

        // Features animation
        const featureCards = document.querySelectorAll('.feature-card');
        if (featureCards.length > 0) {
          gsap.from(featureCards, {
            duration: 0.8,
            y: 50,
            opacity: 0,
            stagger: 0.3,
            ease: "power2.out",
            scrollTrigger: {
              trigger: "#features",
              start: "top 80%"
            }
          });
        }

        // Testimonials animation
        const testimonialCards = document.querySelectorAll('.testimonial-card');
        if (testimonialCards.length > 0) {
          gsap.from(testimonialCards, {
            duration: 0.8,
            y: 50,
            opacity: 0,
            stagger: 0.2,
            ease: "power2.out",
            scrollTrigger: {
              trigger: "#testimonials",
              start: "top 80%"
            }
          });
        }
      } catch (error) {
        console.log('GSAP animation error:', error);
      }
    }
  }

  // Initialize animations after a delay to ensure DOM is ready
  setTimeout(() => {
    initializeAnimations();
  }, 500);
</script>